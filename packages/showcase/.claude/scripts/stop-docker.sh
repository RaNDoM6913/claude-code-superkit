#!/usr/bin/env bash
# Project-specific: replace with your stop-docker script.
#
# This was a thin wrapper that delegated to stop.sh --with-docker.
# It stopped all services AND Docker containers (PostgreSQL, Redis, MinIO).

echo "Replace this stub with your project's stop-docker script."
exit 1
